<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mychimney">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:name=".CustomApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditVPNActivity"></activity>

        <receiver android:name=".MyServiceStatusReceiver"  android:exported="true">
            <intent-filter>
                <action android:name="com.chineseelements.CHIMNEY">
                </action>
            </intent-filter>
        </receiver>
        <service android:name=".MyChimneyService"
            android:process=".ChimneyVPN"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            >
        </service>
    </application>

</manifest>